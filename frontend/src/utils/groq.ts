// Placeholder for Groq summary integration
// Replace mock implementation with real Groq API call when key is available
export async function generateSummary(text: string): Promise<string> {
  // TODO: wire Groq API key and endpoint here
  return `Summary (mock): ${text.slice(0, 120)}...`;
}
